const assert = require('node:assert')

xdescribe('libs: roundFn', () => {
    xdescribe('expansion()', () => {
        xtest('', () => {})
    })

    xdescribe('finnalSwap()', () => {
        xtest('', () => { })
    })

    xdescribe('getNewR()', () => {
        xtest('', () => { })
    })

    xdescribe('pBox()', () => {
        xtest('', () => { })
    })

    xdescribe('sBox()', () => {
        xtest('', () => { })
    })

    xdescribe('splitRTo6BitChunks()', () => {
        xtest('', () => { })
    })

    xdescribe('xorWHitener()', () => {
        xtest('', () => { })
    })
})