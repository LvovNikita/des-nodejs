const assert = require('node:assert')

xdescribe('libs: keys', () => {
    xdescribe('allocateKey()', () => {
        xtest('', () => {})
    })

    xdescribe('generateRoundKeys()', () => {
        xtest('', () => { })
    })
})